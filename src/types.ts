export type Trip = {
    from: string;
    to: string;
    departureLocation: string;
    arrivalLocation: string;
    startDate: string;
    endDate: string;
    departureTime: string;
    arrivalTime: string;
    id: string;
    bookingNr: string;
};
